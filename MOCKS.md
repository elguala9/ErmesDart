# Project Mocks and Test Doubles

This file lists the lightweight test doubles and mocks found across the workspace. Generated `freezed` private classes are listed separately at the end.

## packages/ermes_signaling

- **_SimpleSignalingServer**: implements `IErmesSignalingServer` — lightweight in-memory signaling server used in `test/integration_test.dart`.
  - File: [packages/ermes_signaling/test/integration_test.dart](packages/ermes_signaling/test/integration_test.dart)

## packages/ermes_core

## packages/ermes_test

## packages/ermes_core (id handler)

## Generated private classes (types package)

The following private classes appear in generated `freezed` files inside the `types` package. These are model classes (not mocks), but are listed here for completeness.

- `_SignalData`, `_ReusableOffer`, `_ReusableAnswer`, `_Response`, `_OfferResponse`, `_AnswerResponse`, `_SignalInfoOffer`, `_SignalInfoAnswer`
  - File: [packages/types/lib/src/signaling_types.freezed.dart](packages/types/lib/src/signaling_types.freezed.dart)
- `_MessageRoot`, `_InternalMessage`, `_MessageData`, `_ChunkMessage`, `_ChunkInfo`, `_ServiceMessage`, `_CallbackOnMessageReceived`
  - File: [packages/types/lib/src/ermes_types.freezed.dart](packages/types/lib/src/ermes_types.freezed.dart)

---

If you want, I can:

- Move these reusable test doubles into `packages/ermes_test` for shared use across packages.
- Add small exported wrappers so tests import `package:ermes_test/mocks.dart` instead of file paths.

Updated: automated listing generated by assistant.
