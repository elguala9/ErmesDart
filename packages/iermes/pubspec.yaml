name: iermes
description: Interfaces and abstract classes for the Ermes messaging system
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  meta: ^1.11.0
  ermes_types:
    path: ../types

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
