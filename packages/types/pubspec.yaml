name: ermes_types
description: Type definitions and data structures for the Ermes messaging system
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  meta: ^1.11.0
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  barrel_files_annotation: ^0.2.0

dev_dependencies:
  build_runner: ^2.4.8
  freezed: ^2.4.7
  json_serializable: ^6.7.1
  lints: ^3.0.0
  test: ^1.24.0
