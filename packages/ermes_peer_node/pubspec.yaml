name: ermes_peer_node
description: Test peer node for ErmesDart integration testing with STUN and blockchain signaling
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.9.4 <5.0.0"

dependencies:
  ermes_core:
    path: ../ermes_core
  ermes_signaling:
    path: ../ermes_signaling
  signaling_contract_sdk: "2.0.0"
  stun_shsp: "^0.1.4"
  web3dart: "^3.0.1"
  wallet: "^0.0.18"
  http: "^1.0.0"
