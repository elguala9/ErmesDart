Every GitHub Action Test need to have these features:
- The yml for the action
- a .sh to run to start the test with github

Every test needs to be Linux and Windows compatible

The .sh nomenclature is run-test-github-<name of the test>.sh