name: ermes_dart_workspace
description: Ermes Dart monorepo workspace
publish_to: none

environment:
  sdk: ">=3.2.0 <4.0.0"

dev_dependencies:
  melos: ^7.0.0

scripts:
  build:barrels: melos exec --scope="iermes" -- dart run build_runner build --delete-conflicting-outputs